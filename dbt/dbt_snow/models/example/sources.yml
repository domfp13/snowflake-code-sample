---
version: 2

sources:
  - name: example
    database: |
      {%- if  target.name == "dev" -%} SNOWFLAKE_CODE_SAMPLE
      {%- elif target.name == "prod" -%} SNOWFLAKE_CODE_SAMPLE
      {%- else -%} invalid_database
      {%- endif -%}
    schema: PUBLIC
    tables:
      - name: raw_customer
      - name: raw_orders
      - name: raw_lineitem
